---
description: Take apart one item or teach one topic, grounded in the bank rather than improvised
argument-hint: [question id, task id, or a topic]
---

Explain what is in `$ARGUMENTS`, following the `exam-coach` skill.

- A question id such as `d3-014`: call `key` on it and walk through the key, then each
  distractor and the named reason it fails. Say which task statement it sits under.
- A task id such as `3.2`: call `task` on it and teach the statement, its traps, and the
  worked example if it has one. Then offer two items on it.
- Anything else: find the nearest task statements with `tasks`, teach from those, and say
  plainly when the topic is one section 17 of the guide puts out of scope. `doc outOfScope`
  is the list.

Stay inside the bank and the guide. If neither covers it, say so rather than filling the
gap from general knowledge, and say whether it is out of scope or simply not written yet.
