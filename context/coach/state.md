# Terminal coaching record

What the `exam-coach` skill has seen. The app's own progress lives in the browser and is
not readable from here, so this file covers terminal sittings only and the two will not
agree. Rewritten in place each sitting, never appended to.

No sittings yet. The first `/cert:drill` or `/cert:quiz` fills this in.

## Weak task statements

Nothing recorded.

## Next

Run `/cert:quiz` for a blueprint-weighted set, which is the fastest way to find where the
gaps are. `/cert:drill D1` if you already know the domain you want.
